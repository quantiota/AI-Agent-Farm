# Generated at `synapse generate` — signing key, secrets, live config. Never commit.
synapse-data/
.env

# Rendered by the nginx service (envsubst) / ACME challenges — host artifacts, not source.
nginx/conf.d/default.conf
nginx/.well-known/*
!nginx/.well-known/.gitkeep
