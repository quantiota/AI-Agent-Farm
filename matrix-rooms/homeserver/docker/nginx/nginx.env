# Substituted into default.conf.template by the nginx service (envsubst '$DOMAIN').
# server_name becomes matrix.${DOMAIN}; certs read from /etc/letsencrypt/live/${DOMAIN}/.
DOMAIN=microserver.network
